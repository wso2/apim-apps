steps:
- task: NodeTool@0
  inputs:
    versionSpec: 14.x

- task: Cache@2
  inputs:
    key: 'npm | "$(Agent.OS)" | package-lock.json'
    path: '$(HOME)/.npm'
  displayName: Cache npm

- script: npm ci
  env:
    gitPAT: $(gitPAT)
